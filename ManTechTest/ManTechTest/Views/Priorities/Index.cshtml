@model IEnumerable<ManTechTest.Models.Priority>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Priority1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PriorityLevel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Priority1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PriorityLevel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PriorityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PriorityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PriorityId })
        </td>
    </tr>
}

</table>
